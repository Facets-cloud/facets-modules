spec:
  title: Aurora
  $schema: http://json-schema.org/draft-07/schema#
  type: object
  properties:
    postgres_version:
      type: string
      title: Postgres version
      description: Version of postgres e.g. 13.3
      minLength: 1
    size:
      type: object
      title: Size
      description: Writer reader datastore sizing
      properties:
        reader:
          type: object
          title: reader
          description: Reader
          properties:
            instance:
              type: string
              title: Instance type
              description: The instance type of the node.
              minLength: 1
            instance_count:
              type: integer
              title: Instance count
              description: The number of instances to create
              minimum: 1
          required:
            - instance
            - instance_count
        writer:
          type: object
          title: writer
          description: Writer
          properties:
            instance:
              type: string
              title: Instance type
              description: The instance type of the node.
              minLength: 1
            instance_count:
              type: integer
              title: Instance count
              description: The number of instances to create
              minimum: 1
          required:
            - instance
            - instance_count
  required:
    - size
    - postgres_version
  order:
    - postgres_version
    - size
