clouds:
- aws
description: Dynamodb
flavor: test-dynamodb
imports:
- name: dynamodb_table
  required: true
  resource_address: module.dynamodb.aws_dynamodb_table.this[0]
intent: dynamodb
outputs:
  default:
    type: '@outputs/dynamodb'
sample:
  disabled: true
  flavor: test-dynamodb
  kind: dynamodb
  spec:
    attributes:
      key1:
        name: LockID
        type: S
    hash_key: LockID
  version: 1.0-local-shrinidhiprabhu
spec:
  description: Specifications for DynamoDB Table
  properties:
    attributes:
      additionalProperties:
        properties:
          name:
            description: The name of the attribute
            minLength: 1
            title: Attribute Name
            type: string
          type:
            description: The type of the attribute (S for string, N for number, B
              for binary)
            enum:
            - S
            - N
            - B
            title: Attribute Type
            type: string
        required:
        - name
        - type
        type: object
      description: The attributes of the DynamoDB table
      title: Attributes
      type: object
    hash_key:
      description: The attribute to use as the hash (partition) key
      minLength: 1
      title: Hash Key
      type: string
      x-ui-placeholder: Enter the hash key attribute name
  title: DynamoDB Table Spec
  type: object
version: 1.0-local-shrinidhiprabhu
