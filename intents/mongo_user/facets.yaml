name: mongo_user
type: DB User
displayName: Mongo User
description: Database user for MongoDB instances, defining access permissions and authentication.
outputs:
  - name: default
    type: "@outputs/mongo_user"
