name: postgres_user
type: DB User
displayName: Postgres User
description: User account in PostgreSQL, controlling access to databases and managing privileges.
outputs:
  - name: default
    type: "@outputs/postgres_user"
